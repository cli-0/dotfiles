neovim profile path: ~/.config/nvim/init.vim

TODO: 

md format
create install script 1: for linux 2: for mac

TODO:

create a script that:
	- run a local juypter notebook
	- opens a page 

TODO:


## configure dev environment

install brew in customized location
brew install llvm
brew install openssl
	`sudo chmod +t /private/tmp/`
install cmake
install pyenv
install python with pyenv

config neovim
	- compile neovim with python
	- run install script 
	- configure youcompleteme
	- C++
		- install clang-format for llvm style
		- configure clang-format to auto save on vim
